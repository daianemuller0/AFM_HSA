@page "/despesas/projetos/{Id}"
@attribute [Authorize]
<PageTitle>Projeto · AFM HSA</PageTitle>

<ModuleScaffold Title="@($"Projeto {Id}")"
                Subtitle="Detalhe do projeto e despesas rateadas."
                Icon="folder" />

@code {
    [Parameter] public string? Id { get; set; }
}
